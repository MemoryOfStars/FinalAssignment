已经完成了播放udisk中的歌曲

接下来需要完成先把udisk中的歌曲扫描到一个数组中

后续需要管理一个标签，指明光标的位置，直接抽出数组中的文件名

用system关闭进程再开始进程实现歌曲切换


////////////////////////////////////////////////


写到了'9'按键


////////////////////////////////////////////////

currentSong的管理存在一些问题

如果检查到当前currentSong不符合播放列表长度要求时
要把改动回退到按键之前；；

目前的情况是直接改动currentSong，但是如果越界的话会直接break，
不回退到之前的状态，使用currentSong时会引发一些问题
